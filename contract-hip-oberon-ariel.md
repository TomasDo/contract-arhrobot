# 契约：Arthrobot Hip（Oberon + Ariel）

> 状态：待紫老登确认  
> 用途：约束 TB产品经理 对该 Teambition 项目的理解；后续按「指定迭代」整理软件需求/缺陷并转交 dockerUU。  
> 更新：2026-09-15

---

## 1. 项目基本信息

| 项 | 内容 |
|---|---|
| 项目名 | Arthrobot Hip |
| 组织 | `6035d317d4f17020561b6574` |
| 项目 URL | https://www.teambition.com/project/61cea04f5b390a10ab37979b |
| TB 角色边界 | 只关注**软件**需求与缺陷；硬件 / 机械 / 电气相关可忽略 |

---

## 2. 产品与软件边界（已理解，待确认）

| 软件 | 角色 | 运行环境 | 是否在盯的范围内 |
|---|---|---|---|
| **Oberon** | 术中导航 / 导航软件，运行在手术机器人上 | 机器人侧 | 是 |
| **Ariel** | 术前规划软件，生成 plan 文件给 Oberon 执行 | Windows 独立软件 | 是 |

协作下游：在迭代范围内整理后的软件需求/缺陷 → 转交 **dockerUU** 修改实现（需群聊就绪后再实际派发）。

---

## 3. Teambition 结构（只读摸底结论）

### 3.1 顶层模块

顶部导航可见：任务、迭代、测试、缺陷、甘特图、文件、需求、概览、更多。

- **需求** 与 **缺陷** 是两个独立顶层模块（路径大致为 `/story` 与 `/bug`），不是主要靠标签区分。
- 需求/缺陷页均有「前往『迭代』规划」入口。

### 3.2 需求侧

观察到的分类示例：

- 未分类
- 软件需求
- 机械需求 / 电气需求 / 无效需求（非软件，日常忽略）

软件相关应优先看：**软件需求**分类，以及任务组路径中与 Oberon / Ariel 相关的分组（例如「Ariel V1需求与缺陷 / …」）。

### 3.3 缺陷侧

观察到的分类示例：

- 未分类
- Oberon 缺陷
- Ariel 缺陷

软件缺陷主要通过 **Oberon/Ariel 分类** + **版本字段** + **任务组路径** 识别。

### 3.4 迭代 / 版本命名

迭代命名采用「产品线 + 版本」格式，例如：

- `Oberon V2.10.0`、`Oberon V2.9.1`、`Oberon V2.11.0`
- `Ariel V2.10.0`、`Ariel V2.11.0`

缺陷标题常见版本前缀，例如 `【OberonV1.7.0】…`、`【Oberon2.5.0】…`。

### 3.5 关键字段（详情里常见）

通用：状态、执行者、开始/截止、投入时间、项目路径、备注/描述、子任务、动态。

需求额外：需求类型、需求分类、发现版本、修复版本、迭代、测试计划、相关文件、意见归属、对手术影响、意见来源等。

缺陷额外：缺陷类型、严重程度、缺陷分类、发现/修复版本、迭代、测试计划、相关文件等。

**摸底时注意：** 大量条目的迭代、版本、测试计划为空；分类常为「未分类」；详情「当前状态」与历史动态中的状态文案偶有不一致——自动化/转交前以**当前状态控件与工作流**为准。

---

## 4. 工作约定（按你的说明起草，待确认）

1. **范围由迭代限定**：你指定某个/某些迭代后，我只处理该迭代内的软件需求与缺陷。
2. **不擅自全库扫描派发**：未指定迭代、或群聊未就绪时，不把任务丢给 dockerUU。
3. **转交内容**：任务 ID、标题、类型（需求/缺陷）、所属软件（Oberon/Ariel）、迭代名、当前状态、链接、以及我认为 dockerUU 需要的摘要（复现/目标/验收点——若详情里有）。
4. **本契约**：你可直接在本文件里批注「确认 / 纠正 / 补充」；我按你改完后的版本执行。

---

## 5. 待你确认 / 答疑

请直接在每条后面写答案或改契约正文。

1. **软件边界**：除 Oberon、Ariel 外，Hip 项目里是否还有必须盯的软件（例如 Sirius 等）？还是一律忽略？
2. **「未分类」处理**：指定迭代内若需求/缺陷分类为未分类，但标题或任务组明显是软件，是否仍算软件范围并转交？
3. **优先级**：同一迭代内，默认排序规则是什么？（例如：严重程度 → 崩溃/阻塞 → 状态「待评估/研发修改」→ 创建时间？）
4. **状态过滤**：哪些状态**不要**转交？（例如：已完成、无效、暂不修改、延期处理、持续跟踪？）
5. **Ariel vs Oberon 混在同一迭代**：若一个迭代名是 Oberon，但条目分类是 Ariel（或反过来），以**迭代名**还是**分类/任务组**为准归属给 dockerUU？
6. **字段缺失**：发现版本 / 修复版本 / 严重程度为空时，是否仍转交？转交时是否要求我先补字段还是只备注「字段缺失」？
7. **需求 vs 缺陷**：指定迭代内是两者都转，还是默认只转缺陷、需求另说？
8. **转交粒度**：一条 TB 任务 = 一次给 dockerUU 的一条工单？还是允许我合并同一模块的多条？
9. **验收标准**：需求/缺陷详情里没有明确验收时，转交前是否需要你过一眼，还是允许我根据描述起草「建议验收」并标注待确认？
10. **与测试的关系**：项目里有「测试」模块；转交 dockerUU 改代码时，是否需要同步知会「规划软件测试工程师」或其他角色？（当前默认：只交 dockerUU，除非你另说。）

---

## 6. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-15 | 初稿：基于只读摸底与当面说明整理，待确认 |
